<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.book_tads">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/book_inicio"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SwipeDragDropActivity"></activity>
        <activity android:name=".activity.RecyclerViewActivity" />
        <activity android:name=".activity.ViewHolderActivity" />
        <activity android:name=".activity.Book_LayoutActivity" />
        <activity android:name=".activity.ListViewActivity" />
        <activity android:name=".activity.AutoCompleteActivity" />
        <activity android:name=".activity.PageViewActivity" />
        <activity android:name=".activity.ListBookActivity" />
        <activity android:name=".activity.RegisterBookActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>